# Database (use one of the two options below)

# Option 1: Full connection URL (recommended for Railway, production)
# DATABASE_URL=mysql://USER:PASSWORD@HOST:PORT/DATABASE

# Option 2: Separate variables (for local dev or when URL is not used)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=
# DB_NAME=efootball

# Required: JWT signing secret (use a long random string in production)
JWT_SECRET=your-secret-key

# Optional: Server port (default 4000)
# PORT=4000

# Optional: Allowed CORS origins, comma-separated (e.g. frontend URLs)
# CORS_ORIGIN=https://mksuefootball.vercel.app,http://localhost:3000
